# in project root
rm -rf node_modules package-lock.json pnpm-lock.yaml yarn.lock
npm install                         # should now succeed
git add package.json
git commit -m "remove op-sqlite / react-native – fix React-18 build"
git push origin test                # triggers a clean Vercel build